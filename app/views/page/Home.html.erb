
<div class='container'>
  <div class="row">
    <div class='col-2' >
      <%= render 'shared/categories_list', tags: @categories %>
    </div>
    <div class='col-10' >
      <div class="row ">
        <%= render @films %>
      </div>
    </div>
  </div>
</div>

<% if @pagy.count >= 1 %>
  <div class="d-flex justify-content-center">
    <%== pagy_bootstrap_nav(@pagy) %>
  </div>
<% end %>
